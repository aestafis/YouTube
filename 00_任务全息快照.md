# 任务全息快照（交接版）

## 1) 任务目标（用户口径）
- 将“失败重试”按钮改为与“校验索引”一致的淡灰色（避免浅紫色过亮）。
- 在不破坏现有流程的前提下，把当前可做的稳定性优化补齐。
- 明确回答：功能是否完备、是否可落地。

## 2) 本轮已落地改动（核心）
- 文件：`/home/runner/work/YouTube/YouTube/yt_downloader.py`
- UI样式：
  - `失败重试`按钮颜色改为 `#5d6d7e`，与`校验索引`一致。
- 逻辑稳定性：
  - 进入失败重试准备流程时，先重置`_force_retry_once=False`，避免脏状态残留。
  - 失败重试目标仅保留合法 YouTube `video_id`。
  - 当无有效失败项时，明确日志与状态提示并安全返回。
  - 当 fallback 重试列表渲染后仍无可勾选项时，明确提示并安全返回。
  - 仅在确实存在可重试勾选项时，才开启`_force_retry_once=True`。

## 3) 行为结果（面向接手AI/工程）
- “失败重试”入口保留在设置区，视觉风格与“校验索引”统一。
- 失败重试在以下场景更稳健：
  - 失败池含无效ID；
  - 当前预览无法直接勾选；
  - 重试列表生成后仍无有效可下载项。
- 一次性强制重试标记不会被误开启，失败计数清零时机更可控。

## 4) 验证与状态
- 已执行语法校验：`python -m py_compile yt_downloader.py`（通过）。
- 当前分支最近相关提交：
  - `77687c5 feat: round2 auto-build retry list and lighten retry button`
  - `d3db95f fix: set retry button gray and harden retry activation guards`

## 5) 功能完备性结论（当前快照）
- 对本次需求范围（样式统一 + 失败重试稳态优化）判定为：**完备**。
- 工程落地判定：**可落地**（可交接到下一位AI直接继续维护）。

## 6) 下一位AI建议起手顺序
1. 先读本文件与`01_用户人物画像快照.md`。
2. 再读最新版代码：`/home/runner/work/YouTube/YouTube/yt_downloader.py`。
3. 如需继续增强，优先保持“失败重试”链路可解释性（日志、状态、兜底一致）。
